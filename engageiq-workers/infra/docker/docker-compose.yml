services:
  workers:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    env_file: ../../.env
    ports:
      - "9090:9090"
    restart: unless-stopped
    networks:
      - Mailvex

networks:
  Mailvex:
    name: docker_Mailvex
    external: true
